package com.expense.tracker.enums;

/**
 * Transaction Type Enum
 * 
 * Defines the type of financial transaction:
 * - INCOME: Money coming in (positive cash flow)
 * - EXPENSE: Money going out (negative cash flow)
 */
public enum TransactionType {
    INCOME,
    EXPENSE
}
